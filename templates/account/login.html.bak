<!-- templates/account/login.html -->
{% extends "base.html" %}

{% block title %}Login - Edusuite{% endblock %}

{% block content %}
<div class="login-container">
    <h2>Login to Your Account</h2>
    
    {% if current_school %}
        <div class="school-branding">
            <h3>{{ current_school.name }}</h3>
        </div>
    {% endif %}

    <form method="post" action="{% url 'account_login' %}">
        {% csrf_token %}
        
        {% if form.errors %}
            <div class="alert alert-error">
                Please correct the errors below.
            </div>
        {% endif %}

        <div class="form-group">
            <label for="id_login">Email Address</label>
            <input type="email" name="login" id="id_login" required 
                   placeholder="your@email.com" value="{{ form.login.value|default:'' }}">
            {% if form.login.errors %}
                <div class="error">{{ form.login.errors }}</div>
            {% endif %}
        </div>

        <div class="form-group">
            <label for="id_password">Password</label>
            <input type="password" name="password" id="id_password" required>
            {% if form.password.errors %}
                <div class="error">{{ form.password.errors }}</div>
            {% endif %}
        </div>

        <div class="form-group">
            <label>
                <input type="checkbox" name="remember">
                Remember me
            </label>
        </div>

        <button type="submit" class="btn btn-primary">Sign In</button>

        <div class="auth-links">
            <a href="{% url 'account_signup' %}">Create an account</a> | 
            <a href="{% url 'account_reset_password' %}">Forgot password?</a>
        </div>
    </form>
</div>
{% endblock %}